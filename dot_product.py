# Given a numpy array,calculate the dot product of the array with itself.
import numpy as np
arr = np.array([1, 2, 3, 4, 5])
print("Array:", arr)
print("Dot Product with itself:", np.dot(arr, arr))
